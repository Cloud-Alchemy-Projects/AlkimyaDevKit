import {makeStyles} from "@mui/styles"

export default makeStyles(() => ({
    mainContainer: {
        width: "100%",
        height: "100vh",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center"
    },
}));